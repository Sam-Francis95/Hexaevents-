// Owned by the Organizer module developer.
// Export the nav items this module contributes to the shared Sidebar.
// Shape: { label, path, icon (lucide-react component), roles: string[] }
export const organizerNav = [];
