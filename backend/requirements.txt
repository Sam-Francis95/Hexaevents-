Flask
Flask-JWT-Extended
Flask-Cors
pymongo
bcrypt
google-auth
requests
python-dotenv
Faker

# Test-only, but kept in the main file (same precedent as Faker) so `pip
# install -r requirements.txt` alone is enough to run the test suite.
pytest
mongomock

# Uncomment once LLM_PROVIDER is set to "openai" or "azure":
# openai
